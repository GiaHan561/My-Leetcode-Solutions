class Solution:
    def containsDuplicate(self, nums: List[int]) -> bool:
        appeared = set()
        for i in range(len(nums)):
            if nums[i] not in appeared:
                appeared.add(nums[i])
            else:
                return True

        return False
